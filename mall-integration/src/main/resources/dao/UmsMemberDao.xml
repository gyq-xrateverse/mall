<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.integration.dao.UmsMemberDao">

    <select id="selectByIdForUpdate" resultType="com.macro.mall.model.UmsMember">
        SELECT
            id,
            member_level_id,
            username,
            password,
            nickname,
            phone,
            status,
            create_time,
            icon,
            gender,
            birthday,
            city,
            job,
            personalized_signature,
            source_type,
            integration,
            growth,
            luckey_count,
            history_integration
        FROM
            ums_member
        WHERE
            id = #{id}
        FOR UPDATE
    </select>
</mapper>
